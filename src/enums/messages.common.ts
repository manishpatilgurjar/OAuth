/**
 * Enum representing messages used for communication or error handling.
 */
export enum Messages {
    /**
     * Message for indicating an internal server error.
     */
    INTERNAL_SERVER_ERROR = 'Internal Server Error',

    /**
     * Message indicating that certain parameter(s) cannot be empty.
     */
    EMPTY_FIELDS = 'The following parameter(s) cannot be empty :',
}
